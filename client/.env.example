# client/.env.example
# For local dev the Vite proxy handles /api automatically — you don't need this.
# For Vercel production deployment, create .env.production with your Railway URL:
#
# VITE_API_URL=https://your-server.up.railway.app
#
# In Vercel dashboard: Settings → Environment Variables → add VITE_API_URL
